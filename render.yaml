services:
  - type: web
    name: polychat
    env: node
    buildCommand: cd web && npm install && npm run build
    startCommand: cd web && npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
